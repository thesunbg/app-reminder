DATABASE_URL="postgresql://localhost:5432/family_hub"
PORT=3001
NODE_ENV=development
# openssl rand -hex 32
SESSION_SECRET="thay-bang-chuoi-ngau-nhien-32-byte"
WEB_ORIGIN="http://localhost:5173"
# Tạo bot qua @BotFather trên Telegram, dán token vào đây rồi khởi động lại server.
# Không có token thì kênh Telegram tắt, app vẫn chạy bình thường.
TELEGRAM_BOT_TOKEN=""
# npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:thesunbg@gmail.com"

# Push native cho app Capacitor (iOS/Android) — xem apps/mobile/README.md.
# Dán nguyên một dòng nội dung file service account JSON tải từ Firebase
# Console → Project settings → Service accounts → Generate new private key.
# Để rỗng thì kênh native tắt, Telegram và Web Push vẫn chạy.
FCM_SERVICE_ACCOUNT=""
